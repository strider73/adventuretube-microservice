server:
  port: ${GATEWAY_PORT}

spring:
  application:
  #  cloud:
  #    gateway:
  #      routes:
  #        - id: member-service
  #          uri: lb://MEMBER-SERVICE
  #          predicates:
  #            - Path=/member/**
  #        - id: web-service
  #          uri: lb://WEB-SERVICE
  #          predicates:
  #            - Path=/web/**

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
       defaultZone: ${EUREKA_SERVER_URL}
    fetch-registry: true
    register-with-eureka: true

springdoc:
  swagger-ui:
    urls:
      - name: Auth Service
        url: http://${CLOUD_IP_ADDRESS}:${AUTH_SERVICE_PORT}/v3/api-docs
      - name: Member Service
        url: http://${CLOUD_IP_ADDRESS}:${MEMBER_SERVICE_PORT}/v3/api-docs
      - name: Web Service
        url: http://${CLOUD_IP_ADDRESS}:${WEB_SERVICE_PORT}/v3/api-docs
      - name: Geospatial Service
        url: http://${CLOUD_IP_ADDRESS}:${GEOSPATIAL_SERVICE_PORT}/v3/api-docs



logging:
    level:
      root: INFO
jwt:
  secret: ${JWT_SECRET}
